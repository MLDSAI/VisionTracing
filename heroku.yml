setup:
  addons:
    - plan: heroku-redis
      as: REDIS
  config:
    S3_BUCKET: example-bucket
    LOG_LEVEL: DEBUG
    DEBUG: 1
    FLASK_ENV: development
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
  config:
    FLASK_ENV: development
