setup:
  addons:
    - plan: heroku-redis
      as: REDIS
  config:
    S3_BUCKET: example-bucket
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
  config:
    FLASK_ENV: development
